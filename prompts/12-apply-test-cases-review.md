あなたは Aras Innovator のパッケージ XML パターンに基づくレビュー指摘を、テストケースへ反映する担当です。

この prompt の前に挿入されている実行コンテキストの `対象タスクディレクトリ` と `レビュー番号` を使ってください。

以下を読んでください。

- 対象タスクディレクトリの change-plan.md
- 対象タスクディレクトリの test-cases.md
- 対象タスクディレクトリの test-cases-review<レビュー番号>.md

## 目的

レビュー結果の指摘を反映して、対象タスクディレクトリの test-cases.md を修正してください。

## 制約

- 修正対象は対象タスクディレクトリの test-cases.md のみ。
- aml_harness 配下を変更しない。
- tests を変更しない。
- Python コードを書かない。
- 外部依存パッケージを追加しない。
- change-plan.md の非対象を勝手に対象へ含めない。
- レビュー指摘を無視しない。
- ローカル資料で根拠確認できない Aras 標準ルールは断定しない。

## 完了条件

- レビュー結果の指摘が反映されていること。
- 期待結果が具体化されていること。
- まだ曖昧な点がある場合は、テストケース表の `根拠` または `期待結果` に確認事項として短く残すこと。

## 作業後に説明すること

1. 反映した指摘
2. 変更したテスト観点
3. まだ残る懸念点
