あなたは test-cases.md に基づいて unittest を実装する担当です。

この prompt の前に挿入されている実行コンテキストの `対象タスクディレクトリ` を使ってください。

以下を読んでください。

- 対象タスクディレクトリの change-plan.md
- 対象タスクディレクトリの test-cases.md
- 既存の tests 配下
- 既存の samples 配下

## 目的

test-cases.md のテストケース表に基づいて、先に unittest と必要なサンプル XML を実装してください。

## 更新してよいもの

- tests 配下
- samples 配下

## まだ行ってはいけないこと

- aml_harness 配下を変更しない。
- 実装コードを修正しない。
- 外部依存パッケージを追加しない。
- test-cases.md の非対象を勝手に対象へ含めない。

## 方針

- Python 標準ライブラリの unittest だけを使う。
- 既存テストの書き方に合わせる。
- test-cases.md の No と対応が分かるテスト名またはコメントにする。
- AML パターン例がある場合は、必要に応じて samples 配下に短い XML として追加する。
- テストが失敗してもよい。アプリ実装は次工程で行う。

## 作業後に実行すること

```powershell
python -m unittest
```

失敗した場合も、どのテストがどの理由で失敗したかを説明してください。
