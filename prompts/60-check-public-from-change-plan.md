あなたは実装済みのチェック機能を、public 配下の Aras 標準パッケージ XML で検証する担当です。

この prompt の前に挿入されている実行コンテキストの `対象タスクディレクトリ` を使ってください。

以下を読んでください。

- 対象タスクディレクトリの change-plan.md
- 対象タスクディレクトリの test-cases.md
- aml_harness 配下
- public 配下の Aras 標準パッケージ XML

## 目的

change-plan.md で実装した機能に関係する public 配下の標準パッケージ XML を `python -m aml_harness` で確認し、標準 XML に対して過検出していないか確認してください。

たとえば Form に関する機能であれば、以下のように対象カテゴリを絞って確認してください。

```bash
python -m aml_harness public/PLM/Import/Form/*.xml
```

## 重要な制約

- コードを変更しない。
- tests を変更しない。
- samples を変更しない。
- public 配下を変更しない。
- Markdown の検証結果だけを出力する。
- public の標準 XML は原則として正しい入力として扱い、失敗した場合は過検出候補として原因を分析する。

## 作業

1. change-plan.md から、public 配下で確認すべきカテゴリを判断する。
2. 対象カテゴリの XML に対して `python -m aml_harness` を実行する。
3. 失敗があれば、失敗した XML、エラー内容、原因候補を説明する。
4. 過検出の可能性がある場合は、どのルールが標準 XML の実例と合っていないか説明する。

## 出力形式

以下の形式を必ず守ってください。
特に `## 判定` は必須です。`## 判定` の次の空行の後に、`OK` または `修正が必要` のどちらか1行だけを書いてください。
この形式を省略したり、別の見出し名に変えたりしないでください。

# public 標準パッケージ検証

## 判定

OK または 修正が必要

## 実行対象

実行したコマンド、対象ファイル範囲、対象件数を書く。

## 結果

成功件数、失敗件数を書く。

## 失敗ケース

| No | ファイル | エラー内容 | 原因候補 | 修正方針 |
|---:|---|---|---|---|

## 標準 XML との不整合

## 次のアクション

- OK の場合: Harness 最終検証へ進む。
- 修正が必要な場合: 失敗原因をもとにアプリ実装を見直す。
